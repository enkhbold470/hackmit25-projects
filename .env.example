# Database Configuration
DATABASE_URL=your_database_connection_string

# KnotAPI Configuration (Both Development and Production)
KNOT_CLIENT_ID=your_client_id
KNOT_SECRET=your_secret
NEXT_PUBLIC_KNOT_CLIENT_ID=your_client_id

# Important Notes:
# - NEXT_PUBLIC_KNOT_CLIENT_ID must be set for the frontend SDK to work
# - Restart the development server (pnpm dev) after adding new NEXT_PUBLIC_* variables
# - The same client ID is used for both backend and frontend in development